---
description: Short alias for /fsd:ship-it. Wrap up finished work, cut a version, commit, push, and open a PR.
---

Alias for `/fsd:ship-it`. Invoke the Skill tool with skill name `fsd:ship-it` and pass `$ARGUMENTS` unchanged.
